# Painel Tutory
LOGIN_URL=https://admin.tutory.com.br/login

# Credenciais (campo "Conta" da tela de login)
LOGIN_USER=sua_conta
LOGIN_PASSWORD=sua_senha

# Nomes dos campos do formulário (já corretos para o Tutory)
FIELD_USER=account
FIELD_PASSWORD=password

# Endpoint AJAX de login (opcional; o script lê data-action do HTML)
# LOGIN_ACTION=/intent/login

# Página usada para confirmar sessão autenticada
CHECK_URL=https://admin.tutory.com.br/index
